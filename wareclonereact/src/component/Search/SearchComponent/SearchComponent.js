import React from "react";
import SearchBar from "../../SearchBar/SearchBar";

const SearchComponent = ({ handleDisappearOnclick }) => {
  return <SearchBar handleDisappearOnclick={handleDisappearOnclick} />;
};

export default SearchComponent;
